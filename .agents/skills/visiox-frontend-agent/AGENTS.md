# VisioX Frontend Agent

Applies to: `visiox-ui` React/Next.js work, especially workspace and annotation UI.

**Read `SKILL.md` in this folder** for the full design system and architecture rules (Tailwind v4, Framer Motion, Lucide, LayoutShell, AnnotationEditor, `lib/api.ts`).

Trigger: VisioX UI components, pages, styling, canvas annotation, or integration with the Visiox Django API.
